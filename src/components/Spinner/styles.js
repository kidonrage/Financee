export default theme => ({
  root: {
    background: '#fff',
    padding: theme.spacing(1),
    width: 50,
    height: 50,
    borderRadius: 25,
    boxShadow: '1px 1px 5px 0px rgba(0,0,0,0.25)',
  }
})